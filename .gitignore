# マシンごとに生成する（bootstrap.sh が作る）
# Chrome の場所は環境で違うため、リポジトリには入れない
mermaid-puppeteer.json

# エディタ・OS の一時ファイル
*.swp
*.swo
.DS_Store

# 注意:
#   lazy-lock.json は「除外しない」。
#   プラグインをコミットで固定するためのファイルで、
#   再現性（どのマシンでも同じ版が入る）と安全性（勝手に更新されない）の要。
