# コードビューアー環境 TODO

作成: 2026-08-31 ／ 更新: 2026-09-03

**残っているのは 1（herdr のプラグイン化）だけ。** 2・3 は完了済み。

---

## 1. herdr 連携のプラグイン化（`herdr.nvim`）

対象は `lua/custom/herdr_link.lua`（行数は変動するので都度確認）。**依存ゼロなのでそのまま切り出せる。**
既存プラグインが存在せず、herdr は herdr.dev で公開されているツールなので利用者がいる。

### 決めること（着手前）

- [ ] **キーマップを既定で設定するか**
      今は読み込むと勝手に `Ctrl+L` が設定される。プラグインなら利用者に選ばせるのが作法
- [ ] 名前（`herdr.nvim` / `herdr-link.nvim` など）

### 作業

- [ ] リポジトリ構成に組み替える（`lua/herdr/init.lua` ほか）
- [ ] `setup(opts)` を作り、以下を設定可能にする
      - 送信先の解決順（左隣 / 同タブ / 明示指定 / クリップボード）
      - ペイロードの書式（現在は `【参照コード】` 固定）
      - パスの表記（git ルート相対 / 絶対 / ファイル名のみ）
- [ ] キーマップを既定で張らない形にする（`keys` を返すか、`setup` で任意設定）
- [ ] herdr が無い環境での動作を保証（クリップボードにフォールバック）
- [ ] **コメントの英語化**（現在は全て日本語）
- [ ] `:help` 用ドキュメント（doc/herdr.txt）
- [ ] テスト（plenary/busted）。特に送信先の解決ロジック
- [ ] README（`neighbor_pane_id` の罠など、調べた知見を書くと価値になる）

### 拡張の候補（切り出した後に）

- [ ] 差分のハンク全体を送る
- [ ] ファイル履歴の特定コミットを送る
- [ ] ピッカーの選択結果をまとめて送る
- [x] 送信先を選べるようにする（`:HerdrPick`。同じタブのエージェントから選ぶ）

### 切り出す前に持っていくべき知見

調べて分かったことのうち、README に書くと価値が出るもの。

- `herdr pane neighbor` の戻り値の `pane_id` は**問い合わせた本人**で、
  隣は `neighbor_pane_id`（隣が無いとキーごと存在しない）
- `agent send` は herdr 0.7.5 で廃止された。`pane send-text` は
  新旧どちらにもあり、Enter を送らずに入力欄へ置ける
- 送信先が複数あるとき `pairs` の順序は不定。並べないと毎回変わる
- 末尾の改行は送り先で意味が変わる。エージェント宛なら入力欄で
  行が分かれるだけだが、シェル宛はそのまま実行される
- 矩形選択の桁は**表示桁**。`getpos` が返す**バイト桁**をそのまま
  当てると多バイト文字が壊れる
- `$` で行末まで伸ばした矩形選択では `curswant` が `v:maxcol` になるが、
  `getpos` はそれを運ばないので `getregion` がカーソル桁で切ってしまう


---

## 2. プラグイン化しないものの git 管理・配布 ✅ 完了

結果:

- `git init` 済み。リモートを設定した
- `~/.config/nvim` を symlink にして実体を別の場所に置く構成にも対応した
  （`bootstrap.sh` が実体パスで判定する）
- `scripts/bootstrap.sh` を用意。`NVIM_APPNAME` に対応しており、
  既存の nvim 設定があるマシンには上書きせず並べて入れられる
- `lazy-lock.json` は追跡する（版を固定する要）。
  `mermaid-puppeteer.json` は環境依存なので `.gitignore`
- 手順書を `docs/` に含める

以下は当時の検討メモ（チェックボックスは当時のまま）。

### 調査済みの事実

**環境依存は実質1ファイルだけ**だった。

| ファイル | 問題 | 対処 |
|---|---|---|
| `mermaid-puppeteer.json` | Chrome のパスを直書き（`/usr/bin/google-chrome`） | 起動スクリプトで生成する |
| `lua/custom/md_preview.lua` | `/usr/share/applications` 等 | **探索リストなので問題なし** |

他は全て可搬。`~/.config/nvim` をまるごと管理できる。

### 決めること

- [x] **管理方式を選ぶ**
      - A. `~/.config/nvim` を直接 git リポジトリにする（最も単純）
      - B. 別の場所に置いて symlink（dotfiles にまとめたい場合）
      - C. chezmoi / stow / yadm などのツールを使う
      → **A を推奨。** 対象が nvim だけなら余計な層を挟む必要がない

### 作業

- [x] `~/.config/nvim` で `git init`
- [x] `.gitignore` を書く
      - 除外: なし（`lazy-lock.json` は**含める**。コミット固定＝再現性と安全性のため）
- [x] `mermaid-puppeteer.json` を生成物にする
      - [x] `.gitignore` に追加
      - [x] `scripts/bootstrap.sh` で Chrome のパスを検出して生成
- [x] **`scripts/bootstrap.sh` を書く**（新しいマシンで1回叩けば揃うもの）
      - [x] 依存の導入（neovim / tree-sitter-cli / 言語サーバ / mermaid-cli / marked）
      - [x] `~/.local/share/nvim-md-preview/` へライブラリを複製
      - [x] `mermaid-puppeteer.json` の生成
      - [x] パーサーの導入（`ts.install` を同期実行）
      - [x] 最後に検証コマンドを流して結果を表示
- [x] 手順書をリポジトリに含める（`docs/`）
- [x] README に「新しいマシンでの手順」を3行で書く

### 完成イメージ

```bash
git clone <repo> ~/.config/nvim
~/.config/nvim-viewer/scripts/bootstrap.sh
nvim   # 完了
```


---

## 3. git blame を扱う ✅ 完了

`lua/custom/blame.lua` として自前で実装した（プラグインは増やさなかった）。
`Space g` で切替、`Enter` でその行を変えたコミットの差分、`[h` / `]h` で前後の変更へ。

以下は当時の検討メモ（チェックボックスは当時のまま）。
当初は gitsigns.nvim（案A）を推奨していたが、
「そのファイルの差分だけを見たい」「差分を開いたまま前後のコミットへ移りたい」
という要件が gitsigns では満たせず、案B（自前）を採った。

### 決めること

- [x] **プラグインを足すか、自前で書くか**
      - A. `gitsigns.nvim` を入れる（lewis6991 / 定番 / blame 以外に変更マーカーも付く）
      - B. 自前で `git blame --porcelain` を叩いて表示する（依存を増やさない）
      → **A を推奨。** blame だけでなく、差分マーカーやハンク移動も同時に手に入る。
        ただしプラグインが1つ増える（現在9個 → 10個）
- [x] **常時表示するか、押したときだけか**
      常時だと情報過多になりやすい。**押したときだけ**を推奨

### 作業（A を選んだ場合）

- [x] `gitsigns.nvim` を追加。**閲覧専用の方針に合わせて設定を絞る**
      - [x] ステージ/リセット系のキーマップは**全て無効化**（ファイルを書き換えるため）
      - [x] 変更マーカー（signcolumn）は有効
      - [x] ハンク移動 `]h` / `[h` は有効
- [x] 行 blame を割り当てる（例: `Space g`）
      - [x] カーソル行の blame をポップアップ表示
      - [x] 全ファイル blame の切替も検討
- [x] **Diffview との連携**（ここが肝）
      - [x] blame で出たコミットから、そのコミットの差分を開く
      - [x] `DiffviewOpen <sha>^!` で実現できるはず。要検証
- [x] 3か所に導線を足す
      - [x] フッター（「blame」項目。LSP と同じく状況依存で）
      - [x] コマンドパレット（日英キーワード付き）
      - [x] 早見表
- [x] デモリポジトリに練習用の記述を追加（`JUMPS.md`）
- [x] 手順書2本に反映

### 想定する流れ

```
差分を見る → 気になる行 → Space g で blame
  → 「いつ・誰が・どのコミットで」が出る
  → そのコミットの差分を開く
  → なぜ変えたかコミットメッセージで分かる
```


---

## 着手順の理由

1. **2（git管理）が最優先。** 今後 1 と 3 で設定を変えるが、
   git 管理していないと**変更のたびに手順書を書き直す**必要がある。先にやると後が楽
2. **3（blame）が次。** 単独で完結し、効果が分かりやすい
3. **1（プラグイン化）は最後。** 公開するかの判断が要り、他の2件と独立している

---

## 保留・検討のみ

- UI群（toolbar / statusline / winbar / palette）のプラグイン化
  → 既存プラグインと競合し、個人最適化が強い。**当面は 2 の git 管理で足りる**
- `open_guard.lua`
  → snacks と diffview の相性問題。プラグイン化より **snacks に issue を出す**のが筋
- `md_preview.lua`
  → `markdown-preview.nvim` という定番がある。差別化（オフライン動作）はできるが需要は限定的
